# Dog Age Estimation

本项目是基于深度学习技术训练和评估犬类年龄估计模型。以下是设置环境、下载数据集以及运行训练和评估的完整步骤。

## 测试数据加载

为了确保数据能够正确加载，请运行以下命令：

```bash
python ./datasets/test_dataloader.py
```

## 下载和设置数据集

data
├── annotations
├── trainset
└── valset

## 设置环境
```bash
conda create -n dog_age_estimation python=3.12 -y
conda activate dog_age_estimation

pip install -r requirements.txt
```
## 训练
```bash
./train.sh
```
## 评测
```bash
./eval.sh
```